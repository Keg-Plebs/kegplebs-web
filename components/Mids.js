import sectionStyles from '../styles/Section.module.css';
import { mids } from '../styles/Mids.module.css';

const Mids = () => {
    
    return(
        <div className={`${sectionStyles.main} ${mids}`}>
        </div>
    )
}

export default Mids